\chapter{Compiler and Code Optimization}
\label{ch:compiler-optimization}

\section{Introduction}

A compiler is a program that translates source code written in a high-level programming language into machine code executable by a processor. This translation is not a straightforward one-to-one mapping: modern compilers perform extensive analysis and transformation of the program to produce code that executes faster, consumes less energy, or occupies less memory. These transformations are collectively referred to as \textit{code optimizations}.

The importance of compiler optimization has grown alongside the complexity of both hardware and software. Modern processors feature deep cache hierarchies, wide \gls{simd} (Single Instruction Multiple Data) units, and multiple cores, each introducing new opportunities and constraints for program optimization. Simultaneously, software workloads, particularly in machine learning, have grown in scale and computational intensity, making the quality of compiled code a critical factor in overall system performance.

\section{Phases of a Compiler: From Source Code to Executable}

A compiler typically operates in three major phases: the \textit{front-end}, the \textit{middle-end}, and the \textit{back-end}~\cite{aho2006compilers}. Each phase transforms the program into a progressively lower-level representation.

\begin{figure}[H]
    \centering
    % [PLACEHOLDER: Diagram showing the three-phase compiler pipeline:
    %  Source Code → Front-End (Lexing, Parsing, Semantic Analysis) → IR
    %  → Middle-End (Optimizations) → Optimized IR
    %  → Back-End (Instruction Selection, Register Allocation, Code Emission) → Machine Code]
    \fbox{\parbox{0.7\textwidth}{\centering\vspace{2.5cm} Compiler Pipeline Diagram \\ (Front-End $\rightarrow$ Middle-End $\rightarrow$ Back-End) \vspace{2.5cm}}}
    \caption{The three phases of a compiler: front-end, middle-end, and back-end.}
    \label{fig:compiler-pipeline}
\end{figure}

\subsection{Front-End}

The front-end is responsible for reading the source code and converting it into an intermediate representation (\gls{ir}). This process involves three sub-steps:

\begin{itemize}
    \item \textbf{Lexical analysis} (scanning): The source text is broken into tokens, such as keywords, identifiers, literals, and operators. For instance, the expression \texttt{x = a + 2} is tokenized into \texttt{IDENT(x)}, \texttt{ASSIGN}, \texttt{IDENT(a)}, \texttt{PLUS}, \texttt{INT(2)}.
    \item \textbf{Syntax analysis} (parsing): The sequence of tokens is organized into a parse tree or abstract syntax tree (\gls{ast}) according to the grammar of the source language. This step verifies that the program is syntactically well-formed.
    \item \textbf{Semantic analysis}: The compiler checks for semantic correctness, including type compatibility, variable declarations before use, and scope rules, and annotates the \gls{ast} with type information and symbol table entries.
\end{itemize}

The output of the front-end is an \gls{ir}, typically in a form such as three-address code or static single assignment (\gls{ssa}) form. \gls{ssa} is a representation where each variable is assigned exactly once, which simplifies many subsequent analyses and transformations.

\subsection{Middle-End}

The middle-end operates on the \gls{ir} and is where the majority of optimizations take place. Because the \gls{ir} is independent of both the source language and the target machine, optimizations at this level are portable and can be shared across different language front-ends and hardware back-ends. Middle-end optimizations include:

\begin{itemize}
    \item \textbf{Control-flow optimizations}: constant propagation, dead code elimination, loop-invariant code motion.
    \item \textbf{Data-flow optimizations}: common subexpression elimination, copy propagation, strength reduction.
    \item \textbf{Loop optimizations}: tiling, fusion, interchange, unrolling, vectorization (detailed in Section~1.4).
    \item \textbf{Inter-procedural optimizations}: function inlining, inter-procedural constant propagation.
\end{itemize}

The transformations in our reinforcement learning agent operate at this middle-end level, specifically on the \gls{mlir} Linalg dialect, which represents structured linear algebra operations on buffers and tensors.

\subsection{Back-End}

The back-end translates the optimized \gls{ir} into machine code for a specific target architecture. This phase includes:

\begin{itemize}
    \item \textbf{Instruction selection}: mapping \gls{ir} operations to target machine instructions, often using pattern-matching techniques on a tree representation of the \gls{ir}.
    \item \textbf{Instruction scheduling}: reordering instructions to exploit instruction-level parallelism and avoid pipeline stalls.
    \item \textbf{Register allocation}: assigning program variables to the limited set of physical registers, spilling to memory when necessary.
    \item \textbf{Code emission}: generating the final assembly or object code.
\end{itemize}

While the back-end performs critical low-level optimizations, our work focuses on the middle-end, where high-level loop transformations have the largest impact on memory access patterns and parallelism.

\section{Optimization Paradigms and Techniques}

Compiler optimizations can be categorized along several axes, and they are supported by fundamental program analysis techniques.

\subsection{Machine-Dependent vs. Machine-Independent Optimization}

Optimizations are classified as \textit{machine-independent} when they improve the program without knowledge of the target hardware, and \textit{machine-dependent} when they exploit specific hardware features.

Machine-independent optimizations include constant folding (evaluating constant expressions at compile time), dead code elimination (removing computations whose results are never used), and common subexpression elimination (reusing previously computed values). These transformations are universally beneficial and apply regardless of the target architecture.

Machine-dependent optimizations, by contrast, require knowledge of the target processor. Examples include selecting vector instructions matching the \gls{simd} width of the CPU, choosing tile sizes that fit within a specific cache level, and scheduling instructions to match the pipeline depth of a particular micro-architecture. One of the contributions of this thesis (version V1, described in Chapter~4) is to make the \gls{rl} agent hardware-aware by feeding processor specifications directly into its observation.

\subsection{Static vs.\ Dynamic Optimization}

Optimizations can also be classified by when they are applied. \textit{Static optimization} occurs at compile time, before the program executes. The compiler analyzes the source code or \gls{ir} and applies transformations based on information available statically. Most traditional compiler optimizations fall into this category.

\textit{Dynamic optimization} occurs at runtime, during program execution. Just-In-Time (\gls{jit}) compilers, such as those in Java Virtual Machines or PyTorch's \gls{jit} backend, monitor program behavior and apply optimizations based on observed execution patterns. Dynamic optimization can exploit runtime information, such as branch frequencies, hot paths, and actual data values, that is unavailable at compile time. The trade-off is that the optimization itself consumes runtime resources.

Our \gls{rl}-based auto-scheduler operates as a static optimizer: it selects transformations before execution, guided by features extracted from the \gls{mlir} code. However, the execution-time feedback used during training provides a form of dynamic information that shapes the learned policy.

\subsection{Control-Flow and Data-Flow Analysis}

Before applying any transformation, the compiler must analyze the program to determine whether the transformation is legal (i.e., preserves the program's semantics) and to estimate whether it will be beneficial. Two families of analysis form the foundation of most compiler optimizations.

\textbf{Control-flow analysis} constructs a representation of the possible execution paths through the program. The most common representation is the \textit{control-flow graph} (\gls{cfg}), where nodes represent \textit{basic blocks} (maximal sequences of instructions with a single entry and a single exit) and edges represent possible transfers of control (branches, jumps, fall-through). The \gls{cfg} enables the compiler to reason about dominance (which blocks must execute before others), loops (strongly connected components in the \gls{cfg} with a single entry called the \textit{header}), and reachability.

\begin{figure}[H]
    \centering
    % [PLACEHOLDER: Simple CFG example, e.g., an if-else statement showing basic blocks (B1: condition, B2: then-branch, B3: else-branch, B4: merge) with edges connecting them]
    \fbox{\parbox{0.5\textwidth}{\centering\vspace{2cm} Control-Flow Graph Example \\ (Basic blocks and control-flow edges) \vspace{2cm}}}
    \caption{Example of a control-flow graph for an if-else statement, showing basic blocks and control-flow edges.}
    \label{fig:cfg-example}
\end{figure}

\textbf{Data-flow analysis} determines how values are defined, used, and propagated through the program. Key data-flow problems include:

\begin{itemize}
    \item \textbf{Reaching definitions}: For each program point, which assignments to a variable may have reached that point without being overwritten? This analysis supports constant propagation and common subexpression elimination.
    \item \textbf{Live variable analysis}: For each program point, which variables hold values that may be read before being overwritten? This analysis supports register allocation and dead code elimination.
    \item \textbf{Available expressions}: Which expressions have already been computed and not invalidated? This supports global common subexpression elimination.
\end{itemize}

These analyses are typically formulated as iterative fixed-point computations over a lattice of data-flow facts. In the context of \gls{mlir} and our work, similar analyses, such as dependence analysis for loop transformations, are performed using the polyhedral model or by the \gls{mlir} analysis infrastructure.

\section{Loop Nest Optimization}

Loops account for the vast majority of execution time in compute-intensive programs, particularly in scientific computing and machine learning workloads. A \textit{loop nest} is a set of loops where one loop is contained within the body of another. For example, matrix multiplication involves three nested loops iterating over rows, columns, and the inner product dimension. Optimizing loop nests is therefore one of the most impactful activities a compiler can perform.

This section describes the loop nest transformations that are relevant to our reinforcement learning agent. For each transformation, we explain its mechanism, the conditions under which it is legal and beneficial, and its role in the optimization of \gls{mlir} Linalg operations. The agent's ability to select and parameterize these transformations forms the core of its optimization capability.

\begin{table}[H]
    \centering
    \caption{Summary of loop nest transformations covered in this chapter.}
    \label{tab:loop-transformations}
    \begin{tabularx}{\textwidth}{l >{\raggedright\arraybackslash}p{3.5cm} >{\raggedright\arraybackslash}p{4cm} >{\raggedright\arraybackslash}p{4cm}}
        \toprule
        \textbf{Transformation} & \textbf{Mechanism} & \textbf{Primary Benefit} & \textbf{Legality Condition} \\
        \midrule
        Parallelization & Execute iterations concurrently & Reduced wall-clock time & No loop-carried dependencies \\
        \addlinespace
        Interchange & Swap loop nesting order & Improved memory locality & Dependence vectors remain valid \\
        \addlinespace
        Tiling & Partition iteration space into blocks & Cache utilization & Tile size divides loop bound \\
        \addlinespace
        Fusion & Merge producer-consumer loops & Eliminate intermediate storage & No backward dependencies \\
        \addlinespace
        Vectorization & Replace scalar ops with \gls{simd} & Instruction-level parallelism & Contiguous access, no dependencies \\
        \addlinespace
        Skewing & Transform iteration coordinates & Enable parallelization & Affine schedule exists \\
        \addlinespace
        Unswitching & Hoist loop-invariant conditionals & Reduce branch overhead & Condition is loop-invariant \\
        \addlinespace
        Inversion & Convert while to do-while & Reduce branch count & Loop executes at least once (or guard added) \\
        \addlinespace
        Unrolling & Replicate loop body & Reduce loop overhead, expose \gls{ilp} & Known trip count beneficial \\
        \addlinespace
        Peeling & Extract boundary iterations & Enable alignment, remove guards & N/A (generally always legal) \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Parallelization}

Parallelization distributes the iterations of a loop across multiple processing units (cores or threads) so that they execute concurrently. A loop can be parallelized if its iterations are \textit{independent}, meaning that no iteration reads from or writes to a memory location that is written by another iteration. Loops that carry no data dependencies across iterations are called \textit{parallel loops}, while those that carry dependencies are called \textit{sequential} or \textit{reduction} loops (the latter being parallelizable if the reduction operation is associative and commutative).

In \gls{mlir}, parallelization is expressed through the \texttt{scf.forall} operation, which explicitly marks a loop as parallel and allows the compiler to map its iterations to hardware threads. A related concept is \textit{tiled parallelization}, which combines tiling with parallelization: the loop nest is first tiled, and the outer tile loops are marked as parallel while the inner loops remain sequential. This approach balances parallelism with cache locality.

For our \gls{rl} agent, the \texttt{TiledParallelization} action (denoted \texttt{TP}) applies parallelization to non-reduction loops and tiling to reduction loops within a Linalg operation. If a fusion producer is also specified, the action becomes \texttt{TiledFusion} (\texttt{TPF}), which fuses the producer into the consumer's parallel loop.

\subsection{Interchange (Permutation)}

Loop interchange swaps the order of two nested loops, changing which loop is outermost and which is innermost. The primary motivation for interchange is to improve memory access patterns. When a program traverses a multi-dimensional array, the order of loops determines whether memory accesses are \textit{strided} (accessing distant elements in each iteration) or \textit{contiguous} (accessing adjacent elements).

Consider a matrix stored in row-major order, where elements of the same row are adjacent in memory. If the outer loop iterates over rows and the inner loop iterates over columns, each inner loop iteration accesses \texttt{A[i][j]}, \texttt{A[i][j+1]}, and so on. These accesses are all adjacent in memory, resulting in good spatial locality. If the loops are interchanged, the inner loop accesses \texttt{A[i][j]}, \texttt{A[i+1][j]}, crossing row boundaries and causing poor cache utilization.

Interchange is legal only when the data dependencies of the original loop nest are preserved under the new iteration order. This is verified by dependence analysis: if the direction vectors of all dependencies remain lexicographically non-negative after permutation, the interchange is valid.

In our \gls{rl} system, the \texttt{Interchange} action (denoted \texttt{I}) selects a permutation of the loop indices. The number of possible permutations grows factorially with the number of loops, so the agent must learn which reorderings are beneficial for a given operation. The action space supports three modes: pre-enumerated candidate permutations, pointer-based sequential selection, and continuous Lehmer-code decoding.

\begin{figure}[H]
    \centering
    % [PLACEHOLDER: Two-panel figure showing memory access patterns for a 2D array:
    %  Left: Row-major traversal (inner loop over columns), contiguous accesses, good locality.
    %  Right: Column-major traversal (inner loop over rows), strided accesses, poor locality.
    %  Arrows showing access order in memory layout.]
    \fbox{\parbox{0.8\textwidth}{\centering\vspace{2cm} Loop Interchange: Memory Access Patterns \\ (Contiguous vs.\ Strided Access) \vspace{2cm}}}
    \caption{Effect of loop interchange on memory access patterns. Row-major traversal yields contiguous accesses; column-major traversal causes strided accesses with poor spatial locality.}
    \label{fig:loop-interchange}
\end{figure}

\subsection{Tiling}

Tiling (also called \textit{blocking}) partitions the iteration space of a loop nest into smaller blocks or \textit{tiles}, which are then executed sequentially, with the iterations within each tile executed as before. The primary purpose of tiling is to improve cache utilization: by choosing tile sizes that fit within a cache level, the working set of a tile remains in cache throughout its execution, reducing expensive main memory accesses.

Formally, a loop that iterates over the range $[0, N)$ with step $1$ is tiled into two loops: an outer loop that iterates over tiles with step $T$ (the tile size), and an inner loop that iterates over the elements within the current tile with step $1$. The outer loop iterates $\lceil N/T \rceil$ times. For a nest of $D$ loops, tiling produces $2D$ loops, each of the original $D$ loops contributing an outer tile loop and an inner point loop.

The choice of tile size is critical: tiles that are too large do not fit in cache and provide no benefit; tiles that are too small incur excessive loop overhead and underutilize cache lines. The optimal tile size depends on the cache capacity, the size of the data elements, and the number of arrays accessed in the loop body. In our hardware-aware observation (V1), the agent receives cache size information to inform this choice.

In our system, the \texttt{Tiling} action (denoted \texttt{T}) selects, for each loop in the nest, a tile size from a set of powers of two. A tile size of zero indicates that the loop should not be tiled. The tile size must divide the loop's upper bound for the transformation to be valid.

\begin{figure}[H]
    \centering
    % [PLACEHOLDER: Two-panel figure:
    %  Left: Original iteration space (e.g., 8x8 grid) with arrows showing traversal order.
    %  Right: Tiled iteration space (e.g., 4x4 tiles of size 2x2), with outer tile traversal and inner point traversal shown in different colors/arrows.]
    \fbox{\parbox{0.7\textwidth}{\centering\vspace{2.5cm} Tiling Illustration \\ (Original vs.\ Tiled Iteration Space) \vspace{2.5cm}}}
    \caption{Illustration of loop tiling. The original iteration space (left) is partitioned into smaller tiles (right), executed sequentially at the tile level and within each tile.}
    \label{fig:loop-tiling}
\end{figure}

\subsection{Fusion}

Loop fusion (also called \textit{loop jamming}) merges two or more loops that iterate over the same range into a single loop whose body contains the bodies of all the original loops. The primary benefit is the elimination of intermediate data: when a producer loop writes to a temporary array and a consumer loop reads from it, fusing them replaces the array with a scalar or register-level value, reducing memory traffic and improving locality.

Fusion is legal when there are no backward dependencies between the loop bodies (i.e., the consumer does not depend on a value that the producer computes in a later iteration). In the context of \gls{mlir}, fusion is applied at the operation level: two Linalg operations that are connected by a producer-consumer relationship can be fused into a single operation with a combined loop nest.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[node distance=1.5cm, auto]
        \node[draw, rounded corners] (prod) {Producer (e.g., \texttt{linalg.matmul})};
        \node[draw, rounded corners, right=of prod] (tmp) {Temporary tensor};
        \node[draw, rounded corners, right=of tmp] (cons) {Consumer (e.g., \texttt{linalg.add})};
        \draw[->] (prod) -- (tmp);
        \draw[->] (tmp) -- (cons);
    \end{tikzpicture}
    \caption{Before fusion: intermediate tensor stored in memory.}
    \label{fig:fusion-before}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[node distance=1.5cm, auto]
        \node[draw, rounded corners] (fused) {Fused operation (producer + consumer)};
        \draw[->] (fused) -- ++(0,-0.8);
    \end{tikzpicture}
    \caption{After fusion: no intermediate storage; producer's output is consumed directly.}
    \label{fig:fusion-after}
\end{figure}

Fusion is a key transformation in our system. However, as demonstrated by the case study of the previous team (described in the General Introduction), fusion is not always beneficial: fusing two operations can prevent the individual application of more impactful transformations (such as aggressive parallelization) to each operation. The agent must learn when fusion improves performance and when it hinders other optimizations.

\subsection{Vectorization}

Vectorization transforms a scalar loop that processes one data element per iteration into a vector loop that processes multiple elements simultaneously using \gls{simd} instructions. Modern CPUs support \gls{simd} instruction sets such as SSE (128-bit), AVX2 (256-bit), and \gls{avx}-512 (512-bit), which can operate on 2, 4, 8, or more data elements in a single instruction.

For a loop to be vectorizable, its iterations must be independent (no loop-carried dependencies), the memory accesses must be contiguous (or at least strided with a compile-time-known stride), and the loop body must consist of operations supported by the target \gls{simd} instruction set. Conditional branches within the loop body can be handled through predication, where both branches are executed and the results are masked, but this adds overhead.

In the context of \gls{mlir}, vectorization is applied to Linalg operations through the vector dialect. The \gls{mlir} pipeline progressively lowers Linalg operations to vector operations, which are then converted to \gls{llvm} \gls{ir} and ultimately to \gls{simd} machine instructions. Additional preprocessing steps, such as transposition of convolution operations from NHWC to HWCF layout, and decomposition of large operations into tiles that fit the vector width, may be required for successful vectorization.

In our \gls{rl} agent, the \texttt{Vectorization} action (denoted \texttt{V}) is a terminal action: once vectorization is applied to an operation, no further transformations can be applied to it, and the schedule for that operation is considered complete. The agent also supports automatic vectorization preprocessing for convolution and pooling operations.

\begin{figure}[H]
    \centering
    % [PLACEHOLDER: Two-panel figure:
    %  Left: Scalar execution, one add per instruction, 8 instructions for 8 elements.
    %  Right: SIMD vector execution (e.g., AVX2 256-bit), 4 adds per instruction, 2 instructions for 8 elements.
    %  Show registers and ALU lanes.]
    \fbox{\parbox{0.7\textwidth}{\centering\vspace{2cm} Vectorization: Scalar vs.\ \gls{simd} Execution \\ (e.g., 256-bit AVX2 processing 4 float32 elements per instruction) \vspace{2cm}}}
    \caption{Scalar versus vectorized execution. \gls{simd} instructions process multiple data elements in a single instruction, reducing the total number of instructions executed.}
    \label{fig:vectorization}
\end{figure}

\subsection{Skewing}

Loop skewing transforms the iteration space of a loop nest to enable parallelization of loops that carry dependencies. The basic idea is to change the coordinates of the iteration space so that the dependency direction vectors become parallel to one of the new axes, making the corresponding loop parallelizable.

As an example, consider a loop nest where an array \texttt{A[i][j]} depends on \texttt{A[i-1][j-1]}. In the original iteration space, both loops carry dependencies and cannot be parallelized. By applying the transformation $i' = i$, $j' = i + j$ (skewing the $j$ axis by $i$), the dependency becomes $A[i'][j'-1]$, which is now parallel in the $i'$ dimension.

Skewing is implemented through linear transformations on the iteration space, typically using the polyhedral model~\cite{bacon1993compiler}. While skewing is not currently part of our agent's action space, it is an example of the kind of transformation that a future expansion could incorporate.

\subsection{Loop Unswitching}

Loop unswitching moves a loop-invariant conditional branch from inside a loop to outside, duplicating the loop body for each branch direction. The transformation eliminates the overhead of evaluating the condition in every iteration at the cost of code duplication.

For example, if a loop contains a conditional \texttt{if (debug)} that does not depend on the loop iterator, unswitching produces two copies of the loop: one with the debug code included and one without, with the condition evaluated once before the loop. This optimization is particularly beneficial when the condition is rarely true, as it removes the branch misprediction penalty from the common path.

\subsection{Inversion}

Loop inversion transforms a \texttt{while} loop into a \texttt{do-while} loop by moving the loop condition to the end of the body and adding a guard before the loop for the case where the body should not execute at all. The \texttt{do-while} form reduces the number of branches per iteration from two (condition and back-edge) to one (condition only). This transformation is typically applied at the back-end level and is not part of our agent's action space.

\subsection{Unrolling}

Loop unrolling replicates the body of a loop multiple times, adjusting the stride and trip count accordingly, to reduce loop overhead (branch and counter instructions) and expose instruction-level parallelism. For a loop that iterates $N$ times, applying an unroll factor of $U$ produces a new loop body containing $U$ copies of the original body, with the loop iterating $\lfloor N/U \rfloor$ times. A remainder loop handles the last $N \bmod U$ iterations.

Unrolling is beneficial when the loop body is small and the branch overhead is significant relative to the useful computation. However, excessive unrolling increases code size, which can pressure the instruction cache and cause performance degradation.

In our system, loop unrolling is introduced as one of the expanded action space transformations in version V4 (denoted \texttt{U}). The agent selects an unroll factor from a set of powers of two. Because some operations lose their structured operation tag after unrolling (the operation is lowered to explicit loops that are then unrolled), unrolling is treated as a terminal action: no further transformations can be applied after it.

\subsection{Loop Peeling}

Loop peeling extracts a small number of iterations from the beginning or end of a loop to enable other optimizations. For instance, peeling the first iteration of a loop can remove a conditional branch that is only taken in the first iteration. Peeling can also align the loop body for more efficient vectorization by ensuring that memory accesses start at aligned addresses. While not currently in our action space, peeling complements the unrolling and padding transformations available in version V4.

\section{Challenges in Modern Compiler Optimization}

Despite decades of research and engineering, several fundamental challenges make compiler optimization a difficult, open-ended problem.

\subsection{The Phase Ordering Problem}

The phase ordering problem is the challenge of determining the optimal sequence in which to apply compiler transformations. Transformations are not commutative: applying transformation $A$ before transformation $B$ can produce a different outcome than applying $B$ before $A$, because each transformation modifies the program in ways that affect the applicability and benefit of subsequent transformations.

As a concrete example, consider loop fusion and parallelization applied to a sequence of operations. If fusion is applied first, the two operations are merged, and the resulting combined loop can be parallelized. If parallelization is applied first, each operation is parallelized independently, and fusion may no longer be possible because the parallel loop structure differs. The choice between these two orders depends on the specific operation sizes, the cache hierarchy, and the available parallelism, and there is no universally optimal order.

The search space of possible transformation sequences is combinatorial in the number of transformations and their parameters. For a loop nest with $D$ loops, there are $D!$ possible interchange permutations, $\mathcal{O}(T^D)$ possible tiling configurations (where $T$ is the number of tile size options), and an exponential number of interleavings between different transformation types. Exhaustive search is infeasible, and heuristics often fail to capture the interaction effects between transformations.

This problem is the primary motivation for using reinforcement learning: an \gls{rl} agent can learn, through experience, which sequences of transformations are likely to be effective for a given program structure, without requiring a hand-crafted model of transformation interactions.

\begin{figure}[H]
    \centering
    % [PLACEHOLDER: Diagram showing how the same two transformations (Fusion and Parallelization) applied in different orders to a MatMul+Add sequence produce different outcomes.
    %  Path A: Fusion first → fused op → Parallelization → 12.3x speedup
    %  Path B: Parallelization first on MatMul → no fusion possible → 17.3x speedup
    %  Illustrating the non-commutativity of transformations.]
    \fbox{\parbox{0.85\textwidth}{\centering\vspace{2cm} Phase Ordering Example \\ (Fusion $\circ$ Parallelization $\neq$ Parallelization $\circ$ Fusion) \vspace{2cm}}}
    \caption{Illustration of the phase ordering problem. Applying fusion before parallelization (Path A) yields a different result than parallelization before fusion (Path B). The optimal order depends on the specific program and hardware.}
    \label{fig:phase-ordering}
\end{figure}

\subsection{Trade-offs}

Compiler optimization involves fundamental trade-offs between competing objectives:

\begin{itemize}
    \item \textbf{Performance vs.\ code size}: Optimizations such as loop unrolling and function inlining improve execution speed but increase the size of the generated code. On embedded systems with limited memory, code size may be the primary constraint.
    \item \textbf{Performance vs.\ compilation time}: More aggressive optimization analyses and search procedures take longer. For interactive development, fast compilation is preferred; for production deployment, longer optimization passes are acceptable.
    \item \textbf{Generality vs.\ specialization}: Optimizations tailored to specific program patterns (e.g., matrix multiplication of particular sizes) can achieve higher performance but do not transfer to new patterns. General-purpose optimizations apply broadly but leave performance on the table.
\end{itemize}

Our work focuses on the performance objective, measured through execution time speedup. The trade-off with compilation time is addressed by caching: once a transformation sequence has been evaluated, its execution time is stored and reused if the same sequence is encountered again, amortizing the compilation cost over training and evaluation runs.

\subsection{Modern Hardware Integration}

The diversity and rapid evolution of hardware platforms add another dimension of complexity to compiler optimization. Modern computing environments include heterogeneous systems with CPUs, GPUs, NPUs (Neural Processing Units), and specialized accelerators, each with distinct memory hierarchies, instruction sets, and parallelism models. A transformation sequence that is optimal for an AMD EPYC server processor may be suboptimal for an Intel Xeon or an ARM-based processor, even when the same source program is compiled.

Our hardware-aware observation (V1) is a step toward addressing this challenge: by providing the agent with processor specifications, the learned policy can adapt its optimization decisions to the target platform.

\section{\gls{mlir} Compiler Infrastructure}

The Multi-Level Intermediate Representation (\gls{mlir}) is a compiler infrastructure project within the \gls{llvm} ecosystem, introduced by Lattner et al.~\cite{lattner2021mlir}. \gls{mlir} addresses a key limitation of traditional compiler IRs: the assumption of a single, fixed level of abstraction. Traditional IRs operate at one level (e.g., \gls{llvm} \gls{ir} operates close to machine code, while abstract syntax trees operate close to source code), making it difficult to express transformations that span multiple levels. \gls{mlir} instead supports a \textit{multi-level} \gls{ir}, where different \textit{dialects} define operations, types, and attributes at different levels of abstraction, all coexisting within the same representation.

\subsection{Dialects and Core Concepts}

A dialect in \gls{mlir} is a self-contained namespace of operations, types, and attributes that model a specific domain or abstraction level. \gls{mlir} ships with a set of core dialects that cover common compiler tasks:

\begin{itemize}
    \item \textbf{Linalg} (Linear Algebra): Represents structured operations on buffers and tensors, such as matrix multiplication (\texttt{linalg.matmul}), convolution (\texttt{linalg.conv\_2d}), element-wise addition (\texttt{linalg.add}), and generic operations with user-specified indexing maps (\texttt{linalg.generic}). Linalg operations explicitly encode the loop structure, access patterns, and computation, making them amenable to systematic transformation. All benchmarks in our system are expressed in the Linalg dialect.

    \item \textbf{Affine}: Provides a restricted form of loop nests and memory accesses where loop bounds and array indices are affine (linear) functions of enclosing loop iterators and symbolic constants. The affine dialect enables precise dependence analysis and polyhedral loop transformations.

    \item \textbf{SCF} (Structured Control Flow): Represents standard control flow constructs such as \texttt{scf.for}, \texttt{scf.while}, \texttt{scf.if}, and the parallel loop construct \texttt{scf.forall}. After Linalg operations are lowered to loop form, the resulting loops belong to the \gls{scf} dialect.

    \item \textbf{Vector}: Represents \gls{simd} operations on fixed-size vectors, providing a target for vectorization transformations.

    \item \textbf{MemRef}: Represents memory references, which are typed pointers to multi-dimensional arrays with optional layout maps. MemRef is the type used for buffer-based computations.

    \item \textbf{LLVM}: A direct mapping of the \gls{llvm} \gls{ir} into \gls{mlir}, enabling a seamless transition from high-level \gls{mlir} dialects to the \gls{llvm} back-end for code generation.

    \item \textbf{Transform}: A meta-dialect that encodes transformation schedules as operations. A \texttt{transform.named\_sequence} describes a sequence of transformations (e.g., match an operation, tile it, vectorize it) that are applied to a target \gls{ir} module. The transform dialect is the mechanism through which our \gls{rl} agent's chosen actions are applied to \gls{mlir} code.
\end{itemize}

\begin{table}[H]
    \centering
    \caption{Summary of core \gls{mlir} dialects used in this work.}
    \label{tab:mlir-dialects}
    \begin{tabularx}{\textwidth}{l >{\raggedright\arraybackslash}p{5cm} >{\raggedright\arraybackslash}p{5cm}}
        \toprule
        \textbf{Dialect} & \textbf{Purpose} & \textbf{Role in Our System} \\
        \midrule
        Linalg & Structured linear algebra operations & Source representation of all benchmarks \\
        \addlinespace
        Affine & Polyhedral loops with affine constraints & Intermediate lowering; dependence analysis \\
        \addlinespace
        \gls{scf} & Structured control flow (for, if, forall) & Loop representation after lowering \\
        \addlinespace
        Vector & Fixed-size \gls{simd} vector operations & Target for vectorization transformations \\
        \addlinespace
        MemRef & Typed multi-dimensional memory references & Buffer type throughout the lowering pipeline \\
        \addlinespace
        \gls{llvm} & Direct mapping of \gls{llvm} \gls{ir} in \gls{mlir} & Final stage before \gls{jit} compilation \\
        \addlinespace
        Transform & Transformation schedule encoding & Mechanism for applying agent-selected actions \\
        \bottomrule
    \end{tabularx}
\end{table}

An \gls{mlir} module is a container for operations, which may themselves contain regions of nested operations. This hierarchical structure, combined with the dialect system, allows the same module to contain Linalg operations describing a neural network layer, vector operations representing the \gls{simd} instructions generated for that layer, and \gls{llvm} operations ready for code generation, all within a single, coherent representation.

\begin{figure}[H]
    \centering
    % [PLACEHOLDER: Vertical stack diagram showing progressive lowering through MLIR dialects:
    %  Linalg → Affine → SCF/OpenMP → Vector → LLVM
    %  With arrows between levels and annotation of what each lowering step accomplishes.]
    \fbox{\parbox{0.7\textwidth}{\centering\vspace{3cm} \gls{mlir} Dialect Stack \\ (Progressive Lowering: Linalg $\rightarrow$ Affine $\rightarrow$ \gls{scf} $\rightarrow$ Vector $\rightarrow$ \gls{llvm}) \vspace{3cm}}}
    \caption{Progressive lowering through \gls{mlir} dialects. High-level Linalg operations are gradually lowered through successively more concrete dialects until reaching the \gls{llvm} dialect for code generation.}
    \label{fig:mlir-dialect-stack}
\end{figure}

\subsection{\gls{mlir} and Automated Optimization}

Several properties of \gls{mlir} make it well-suited for automated optimization via reinforcement learning:

\begin{enumerate}
    \item \textbf{Explicit loop structure}: Linalg operations explicitly encode the loops, access patterns, and computations of an operation, providing the agent with a complete and structured description of the optimization target.
    \item \textbf{Progressive lowering}: \gls{mlir} supports the gradual lowering of operations through the dialect stack. The agent can apply transformations at the Linalg level, then lower the result through progressively more concrete dialects (Affine, \gls{scf}, Vector, \gls{llvm}) to obtain executable code.
    \item \textbf{Python bindings}: \gls{mlir} provides comprehensive Python bindings that allow the construction, manipulation, and execution of \gls{mlir} modules from Python, the language in which our \gls{rl} agent is implemented. The bindings also expose the transform dialect interpreter, which applies transformation sequences to \gls{mlir} modules programmatically.
    \item \textbf{Extensibility}: New dialects and transformations can be added to \gls{mlir} without modifying the core infrastructure. This allows our expanded action space (padding, packing, unrolling) to be implemented using existing \gls{mlir} transformation primitives without changes to the \gls{mlir} codebase.
\end{enumerate}

\subsection{Execution Pipeline}

To measure the performance impact of a transformation sequence, our system compiles and executes the transformed \gls{mlir} code. The pipeline proceeds as follows:

\begin{enumerate}
    \item The transformed \gls{mlir} module (containing the sequence of Linalg transformations selected by the agent) is processed through a series of lowering passes: Linalg to loops, \gls{scf} to OpenMP, vectorization, buffer deallocation, and conversion to the \gls{llvm} dialect.
    \item The resulting \gls{llvm} dialect module is translated to \gls{llvm} \gls{ir} and \gls{jit}-compiled using \gls{mlir}'s ExecutionEngine at optimization level \texttt{-O3}.
    \item The compiled function is invoked through a timed wrapper that measures execution time in nanoseconds. The function operates on zero-initialized buffers to eliminate data-dependent variability, and a warm-up invocation precedes the timed invocation to amortize \gls{jit} compilation overhead.
\end{enumerate}

This execution pipeline is the evaluation mechanism for the reward function discussed in Chapter~4.

\section{Conclusion}

This chapter has introduced the fundamentals of compiler code optimization that form the technical foundation of this thesis. We described the three-phase compiler pipeline (front-end, middle-end, back-end), surveyed the classification of optimizations (machine-dependent vs.\ independent, static vs.\ dynamic), and introduced the control-flow and data-flow analyses that enable safe and informed transformations.

The core of the chapter focused on loop nest optimization, detailing the transformations that constitute the action space of our reinforcement learning agent: parallelization, interchange, tiling, fusion, vectorization, and the additional transformations (unrolling, padding, packing, skewing, unswitching, inversion, peeling) that extend the scope of compiler optimization. For each transformation, we explained its mechanism, legality conditions, and performance implications.

We then discussed the fundamental challenges that motivate the automation of compiler optimization: the phase ordering problem, the trade-offs between competing objectives, and the complexity introduced by diverse and evolving hardware platforms. These challenges explain why hand-crafted heuristics are insufficient and why a learning-based approach is attractive.

Finally, we introduced \gls{mlir}, the compiler infrastructure on which our work is built. \gls{mlir}'s multi-level \gls{ir}, dialect system, explicit operation structure, and Python bindings make it uniquely suitable as a platform for \gls{rl}-based optimization. The Linalg dialect, in particular, provides the structured representation of linear algebra operations that our agent observes and transforms, while the transform dialect provides the mechanism for applying those transformations.

The optimization challenges described in this chapter, especially the phase ordering problem and the vast combinatorial search space of possible transformation sequences, motivate the use of reinforcement learning. This approach is capable of learning effective strategies through interaction with the environment rather than relying on pre-programmed rules. The next chapter introduces the concepts and algorithms of reinforcement learning that underpin our approach.
